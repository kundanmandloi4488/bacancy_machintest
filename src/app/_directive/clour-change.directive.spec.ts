import { ClourChangeDirective } from './clour-change.directive';

describe('ClourChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new ClourChangeDirective();
    expect(directive).toBeTruthy();
  });
});
